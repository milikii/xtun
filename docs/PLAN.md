# xtun 当前推进计划

> 更新日期：2026-09-15。批次 C/D/E 的安装身份、节点参数与独立导出、证书生命周期代码已实现，W13 增加原生传输、历史迁移和系统矩阵。
> 当前实现及验证结果见[批次 C/D/E 报告](REPORT-2026-09-15-CDE.md)。后续主要是公共 ACME、强制断电、真人/三端、真实 Cloudflare/ECH/H3 和持续观察；隔离测试不替代这些验收。
> 提交和 push 已获授权。本机生产保持现场，测试 VPS 的 SSH 密码与配置保持不变；不创建或移动 tag，不发布 release。

> 2026-09-17 接手核验：已从 anyrouter 最新有效会话恢复进度，并补齐 C/D/E 报告。运行清单与 9 月 15 日最终证据一致，最终 smoke/PTY 和 VPS 证据归档已完成；候选 `27aa967` 已提交并 push，CI 结果见报告。本机生产快照一致；测试 VPS 已使用旧会话凭据重连，托管文件及服务状态与最终快照一致，当前无 pending，SSH 文件和密码未变。完成准确候选 CI 后按下列外部验收顺序推进。

## 1. 当前结论与基线

W01–W06、W08.1 及批次 A/B 的已有成果保留。本轮从 `ae8c3b3` 接续未提交实现，完成 C/D/E 及其直接回归，不再把安装身份、原生 JSON 导出或续证共锁列为未实现功能。

| 对象 | 当前可确认事实 |
| --- | --- |
| 脚本 / state / 参数 | 声明 `1.1.0`，state schema `2`，参数修订 `2`；没有虚构新 release |
| 核心依据 | `v26.9.9 / 52a412d9e2f5c2a5142b1b4e2ab3771dacb8b120`；默认发现仍为最新非 draft 发布，包含预发布 |
| 安装身份 | bundle 完整运行文件清单和来源；核心二进制、geo 摘要、能力/权限及官方归档身份；可信相同产物 noop，身份未知需显式重装 |
| 节点交付 | 节点 1–9 共用规范对象；URI/PNG/原生 JSON、current/plain/ech、独立导出和二维码重建已实现 |
| 参数迁移 | 新版核心写 users，兼容读取 clients；保留旧身份、flow、Encryption 和可识别的旧客户端调优 |
| 证书 | 人工/自动共锁，候选成对校验与恢复，核对 nginx 实际供证；同域名换证保留 Xray 配置和 URI/PNG，只 reload nginx |
| 自动验证 | 252 组 canonical smoke、两套 PTY、原生双向传输、PNG 解码、三组准确历史 bundle 迁移；环境与结果以报告为准 |
| CI | baseline 增加迁移、原生传输和官方容器导出校验；安装矩阵为 Debian 12/13、Ubuntu 24.04。schedule/dispatch 另跑 latest 发现、语义/传输和安装 |
| 本机生产 | bundle `0.11.14`（声明 schema 1），实际 state `2`，Xray `26.3.27 / d2758a0 / arm64`；不迁移、不猜 state v2 来源 |
| 测试 VPS | `194.195.251.247`，Debian 12 amd64；nginx 实际 `1.30.1`、dpkg 仍为 `1.22.1-9+deb12u9`，既有差异保留并记录 |

代码支持、核心配置校验、隔离传输、真实域名/设备可用和正式发布分别记录。安装身份回执是本机校验记录，不是上游数字签名。

## 2. 阅读入口

| 文档 | 用途 |
| --- | --- |
| [批次 C/D/E 报告](REPORT-2026-09-15-CDE.md) | 当前实现、运行摘要、自动/VPS 验证、失败修正及剩余验收 |
| [详细推进计划](PLAN-UX-RELIABILITY.md) | W01–W17 的完整目标、当前结算、依赖与 G1–G5 |
| [交互与可靠性决策](DECISIONS-UX-RELIABILITY.md) | 身份、恢复、参数、导出、证书、测试和统一发布决策 |
| [参数契约](PARAMETERS.md) | v26.9.9 字段、节点层级、新旧参数及 ECH 的作用范围 |
| [测试 VPS 与三端手册](TEST-VPS-RUNBOOK.md) | 自动套件复现、迁移、NAS 容器命令及仍需执行的真人/网络步骤 |
| [README](../README.md) | 已实现命令、日常维护和用户操作 |
| [批次 B 报告](REPORT-2026-09-14-BATCH-B.md) | 菜单、单节点取用、H3 意图及当时双架构/CI 证据 |
| [W04-R/W05-R 接手报告](REPORT-2026-09-14-W04R-W05R.md) | 前序持久恢复、归属、真实验收与测试机事件 |

本页为当前入口。历史报告不改写；Xray 字段回到 `.claude/skills/xray-core-official-knowledge/docs/stable/config/` 与相应版本 `source/` 核对。

## 3. 已交付与待完成

| 范围 | 结算 |
| --- | --- |
| A：W01-R/W02-R/W04-R/W05-R | 保留已通过的退出/确认、首次原件、持久恢复、草稿身份及权限证据；G1 留强制断电 |
| B：W07/W11.1/W09.1 | 菜单/预览/已有产物取用、H3 意图与本地能力判定已交付；真人及公网 H3 未代签 |
| C：W08.2/8.3 | 安装身份、固定提交引导、可信同版本 noop、显式重装与失败恢复已实现 |
| D：W09 余项/W10/W11.2/11.3 | 节点 7/9、users/xmux/ECH、同源产物、原生 JSON 和独立导出已实现；GUI/公网路径另验收 |
| E：W12 | 共锁回调、staging、实际供证、失败恢复和事件记录已实现；公共 ACME 签发/定时续期仍需受控域名 |
| W13 | 扩展 smoke/PTY、原生五节点与 ECH 参照、三组历史迁移及 CI；准确结果见当前报告 |
| F：W14/W15 | 等待真人、三端设备、真实 Cloudflare 域名及目标网络，仍须两轮完整验收 |
| G：W16 | 在已验收固定组合上完成 72 小时和 7 天观察，再准备正式发布；生产迁移另排 |
| W17 | PQ/额外签名、性能和低优先级静态站研究后置 |

G3 的代码与隔离验证已补齐，但公共 ACME 等外部证据未到位时不整体签字；G1/G2/G4/G5 同样没有提前关闭。

## 4. 下一轮工作顺序

1. 核对本报告所在提交、CI、运行摘要与测试 VPS pending，保留当前恢复点。
2. 用受控公共域名验证 ACME 真正签发、自动到期续期、回调失败/重试和公网实际供证。记录 CA、acme.sh 版本及证书指纹，不只看退出码。
3. 准备云控制台和电源控制，在持久 pending 后、文件替换中和提交决定后分别强制断电，再验证新进程恢复；SIGKILL 和 guest reboot 不替代。
4. 设备到位后执行 W14.1/W15.1，再修正问题、执行 W14.2/W15 全矩阵。先普通 1/3，再五节点、split、ECH、IPv6/H3、NAS 和 WARP。
5. 固定已验收组合进入 W16 的观察与演练；全部必要闸门通过后再定正式版本/tag，单独准备本机生产迁移窗口。

不缩减 ECH/NAS/证书/生命周期范围，也不因缺设备而停止独立软件工作。`1.2.0` 仍只是暂定完整候选名称；不提前发布 `1.1.1`。

## 5. 维护和迁移约束

- `upgrade` 只替换核心与 geo，`update-script` 只替换 bundle/入口；不会顺便迁移服务器配置。旧安装更新候选后，在明确连接中断窗口执行 `apply-config`，才提交 users/参数修订和新产物。
- 相同版本但旧安装没有有效回执时，普通升级拒绝覆盖；按提示加 `--reinstall`，仍执行候选校验和同代恢复。
- `export-client` 只写选定目标，`rebuild-qr` 只重建 QR 目录。导出持锁并检查当前代；重建逐字核对文档 URI，旧定义不一致时要求先明确迁移。
- 旧 xmux 自定义/来源未知的值保留；新装省略核心默认字段。旧 ECH 值保持，已移除的 force 选项不能再传入。
- 有 pending 先检查并运行 `recover [--yes]`；恢复成功不等于原操作成功。系统包、外部注册、日志、实时网络和内核不承诺文件级回滚。
- 本机生产不迁移；VPS 不改 SSH 密码/配置；公开文档不放令牌、私钥、完整节点 URI 或密码哈希正文。

## 6. 历史索引

[W01/W02](REPORT-2026-09-14-W01-W02.md)、[W03](REPORT-2026-09-14-W03.md)、[W04](REPORT-2026-09-14-W04.md)、[W08.1](REPORT-2026-09-14-W08.1.md)、[W05](REPORT-2026-09-14-W05.md)、[W06](REPORT-2026-09-14-W06.md)、[9 月 14 日复核](REVIEW-2026-09-14.md)、[9 月 13 日复审](REVIEW-2026-09-13.md)、[旧生产就绪计划](PLAN-PRODUCTION-READINESS.md)与 [1.1.0 归档](archive/PLAN-1.1.0.md)保持原证据边界。
